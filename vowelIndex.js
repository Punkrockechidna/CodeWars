var word = "hello";
vowelIndices(word);

function vowelIndices(word) {
    var indices = [];
    word.array.forEach(element => {
        switch (element) {
            case "a":
                indices.push(i+1);
                break;

            default:
                break;
        }
    })
}